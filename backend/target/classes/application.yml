server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/webgissy
    username: postgres
    password: pg123456

jpa:
  hibernate:
    ddl-auto: none
  show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect